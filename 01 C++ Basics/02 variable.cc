//Variable is a container that stores various types of data in program.
#include<iostream>
using namespace std;
int main()
{
    char Name='A';
    int Age=20;
    cout<<"Name in character form="<<Name;
    cout<<"\nAge="<<Age;
}
//Variable can not be starts from number.so "1Name" is not true but "Name1" is always true.

//space is not include in variable name.so "Na me" is not true but "Name" is true.

//we cannot use special symbol or inverted commas for variable name.so "Name@12" is not true
